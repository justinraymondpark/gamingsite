rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Screenshots folder - public read, authenticated write
    match /screenshots/{allPaths=**} {
      allow read: if true;
      allow write: if true; // You can add authentication later
      allow delete: if true; // You can add authentication later
    }
  }
}
